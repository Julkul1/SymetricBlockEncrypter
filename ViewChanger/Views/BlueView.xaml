<UserControl x:Class="SymetricBlockEncrypter.Views.BlueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SymetricBlockEncrypter.ViewModels" d:DataContext="{d:DesignInstance Type=local:BlueViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Blue">
        <Border BorderBrush="Black" BorderThickness="1">
            <Label HorizontalAlignment="Left" Margin="21,70,0,0" VerticalAlignment="Top" Content="{Binding ArtistName}" Width="245" Height="84"/>
        </Border>
        <Button Content="Button" HorizontalAlignment="Left" Margin="60,48,0,0" VerticalAlignment="Top" Width="75" Command="{Binding ChangeNameCommand}"/>
    </Grid>
</UserControl>
